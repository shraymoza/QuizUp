# Example Terraform variables file
# Copy this to terraform.tfvars and fill in your values
# terraform.tfvars is in .gitignore and will not be committed

bucket_name      = "quizup-shray-123456" # must be globally unique
region           = "us-east-1"
sm_endpoint_name = "quizup-sm-endpoint"

# Amplify Configuration
# Uncomment and fill in when ready to deploy to Amplify
# amplify_repository_url = "https://github.com/yourusername/quizup.git"
# github_oauth_token     = "your_github_token_here"  # Optional, only if using GitHub
# custom_domain          = "quizup.example.com"       # Optional

